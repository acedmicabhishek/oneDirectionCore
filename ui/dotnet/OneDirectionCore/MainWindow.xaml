<Window x:Class="OneDirectionCore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="oneDirectionCore" Height="820" Width="440" 
        Background="#121212" Foreground="#EEEEEE"
        WindowStyle="SingleBorderWindow" ResizeMode="CanMinimize" 
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- General Styles -->
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#BBBBBB"/>
        </Style>

        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00D2B4"/>
            <Setter Property="Margin" Value="0,15,0,8"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#00D2B4"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Custom Slider Style - Modern Teal -->
        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Background="#2A2A2A" Height="4" CornerRadius="2" VerticalAlignment="Center" Margin="5,0"/>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb Width="14" Height="14">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="#00D2B4" Stroke="#121212" StrokeThickness="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Robust ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          Foreground="{TemplateBinding Foreground}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Padding="{TemplateBinding Padding}"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="false"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Margin="6,2,2,2" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                                <Path Name="Arrow" Grid.Column="1" Fill="#888888" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M0,0 L4,4 L8,0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#2A2A2A" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="#1A1A1A" BorderBrush="#444444" BorderThickness="1" CornerRadius="0,0,2,2"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True" VerticalScrollBarVisibility="Auto">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#00D2B4"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
        </Style>
    </Window.Resources>

    <!-- Hidden scrollbar while maintaining scrollability -->
    <ScrollViewer VerticalScrollBarVisibility="Hidden">
        <StackPanel Margin="24">
            
            <TextBlock Text="oneDirectionCore" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,2"/>
            <TextBlock Text="Spatial Audio Radar Controller" Foreground="#666666" FontSize="11" Margin="0,0,0,15"/>
            
            <Separator/>

            <!-- Engine Section -->
            <TextBlock Text="ENGINE" Style="{StaticResource Header}"/>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnStart" Content="START" Style="{StaticResource PrimaryButton}" Click="BtnStart_Click" Width="100"/>
                <Button x:Name="BtnStop" Content="STOP" Click="BtnStop_Click" Width="80"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Border x:Name="StatusIndicator" Width="10" Height="10" CornerRadius="5" Background="#555555" VerticalAlignment="Center" Margin="2,0,8,0"/>
                <TextBlock x:Name="StatusLabel" Text="ENGINE STOPPED" FontSize="10" VerticalAlignment="Center"/>
            </StackPanel>

            <Separator/>

            <!-- Audio Section -->
            <TextBlock Text="AUDIO PIPELINE" Style="{StaticResource Header}"/>
            
            <TextBlock Text="Input Channels"/>
            <ComboBox x:Name="ComboChannels">
                <ComboBoxItem IsSelected="True">Stereo (2.0)</ComboBoxItem>
                <ComboBoxItem>Surround (5.1)</ComboBoxItem>
                <ComboBoxItem>Surround (7.1)</ComboBoxItem>
            </ComboBox>

            <TextBlock Text="Detection Preset"/>
            <ComboBox x:Name="ComboPreset">
                <ComboBoxItem IsSelected="True">Standard / Raw Energy</ComboBoxItem>
                <ComboBoxItem>PUBG Advanced</ComboBoxItem>
            </ComboBox>

            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Text="Sensitivity" Grid.Column="0"/>
                        <TextBlock x:Name="LblSensitivity" Text="70" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                    </Grid>
                    <Slider x:Name="SliderSensitivity" Minimum="0" Maximum="100" Value="70" ValueChanged="SliderSensitivity_ValueChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Text="Separation" Grid.Column="0"/>
                        <TextBlock x:Name="LblSeparation" Text="50" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                    </Grid>
                    <Slider x:Name="SliderSeparation" Minimum="0" Maximum="100" Value="50" ValueChanged="SliderSeparation_ValueChanged"/>
                </StackPanel>
            </Grid>

            <Separator/>

            <!-- Display Section -->
            <TextBlock Text="DISPLAY &amp; OVERLAY" Style="{StaticResource Header}"/>
            
            <CheckBox x:Name="CheckFullscreen" Content="Full Screen Overlay" Margin="0,0,0,15"/>

            <TextBlock Text="Radar Position"/>
            <ComboBox x:Name="ComboPosition">
                <ComboBoxItem>Top-Left</ComboBoxItem>
                <ComboBoxItem IsSelected="True">Top-Center</ComboBoxItem>
                <ComboBoxItem>Top-Right</ComboBoxItem>
                <ComboBoxItem>Bottom-Left</ComboBoxItem>
                <ComboBoxItem>Bottom-Center</ComboBoxItem>
                <ComboBoxItem>Bottom-Right</ComboBoxItem>
            </ComboBox>

            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="Radar HUD Size" Grid.Column="0"/>
                <TextBlock x:Name="LblRadarSize" Text="280" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
            </Grid>
            <Slider x:Name="SliderRadarSize" Minimum="100" Maximum="600" Value="280" ValueChanged="SliderRadarSize_ValueChanged"/>

            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="Global Overlay Opacity" Grid.Column="0"/>
                <TextBlock x:Name="LblGlobalOpacity" Text="100%" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
            </Grid>
            <Slider x:Name="SliderGlobalOpacity" Minimum="0" Maximum="100" Value="100" ValueChanged="SliderGlobalOpacity_ValueChanged"/>

            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="Radar HUD Opacity" Grid.Column="0"/>
                <TextBlock x:Name="LblRadarOpacity" Text="80%" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
            </Grid>
            <Slider x:Name="SliderRadarOpacity" Minimum="0" Maximum="100" Value="80" ValueChanged="SliderRadarOpacity_ValueChanged"/>

            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="Entity / Dot Opacity" Grid.Column="0"/>
                <TextBlock x:Name="LblDotOpacity" Text="100%" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
            </Grid>
            <Slider x:Name="SliderDotOpacity" Minimum="10" Maximum="100" Value="100" ValueChanged="SliderDotOpacity_ValueChanged"/>

            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Text="Zoom / Range" Grid.Column="0"/>
                        <TextBlock x:Name="LblRange" Text="100" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                    </Grid>
                    <Slider x:Name="SliderRange" Minimum="1" Maximum="200" Value="100" ValueChanged="SliderRange_ValueChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Text="Max Entities" Grid.Column="0"/>
                        <TextBlock x:Name="LblMaxEntities" Text="6" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                    </Grid>
                    <Slider x:Name="SliderMaxEntities" Minimum="1" Maximum="10" Value="6" ValueChanged="SliderMaxEntities_ValueChanged"/>
                </StackPanel>
            </Grid>
            
            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="Polling Rate (Hz)" Grid.Column="0"/>
                <TextBlock x:Name="LblPollRate" Text="144 Hz" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
            </Grid>
            <Slider x:Name="SliderPollRate" Minimum="30" Maximum="240" Value="144" ValueChanged="SliderPollRate_ValueChanged"/>

            <Separator/>

            <!-- Hardware Section -->
            <TextBlock Text="HARDWARE" Style="{StaticResource Header}"/>
            <TextBlock Text="Serial COM Port"/>
            <ComboBox x:Name="ComboPorts"/>

            <Separator Margin="0,20,0,10"/>
            <Button Content="Exit Application" HorizontalAlignment="Right" Background="#331111" Click="BtnExit_Click" Width="120" Height="30"/>
        </StackPanel>
    </ScrollViewer>
</Window>
