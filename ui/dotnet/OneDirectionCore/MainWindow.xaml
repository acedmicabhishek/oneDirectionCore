<Window x:Class="OneDirectionCore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="oneDirectionCore" Height="620" Width="920" 
        Background="#121212" Foreground="#EEEEEE"
        WindowStyle="SingleBorderWindow" ResizeMode="CanMinimize" 
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- General Styles -->
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#BBBBBB"/>
        </Style>

        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00D2B4"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#353535"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#151515"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#00D2B4"/>
            <Setter Property="Foreground" Value="#121212"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#33E2CD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Custom Slider Style - Modern Teal -->
        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Background="#2A2A2A" Height="4" CornerRadius="2" VerticalAlignment="Center" Margin="5,0"/>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb Width="16" Height="16">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="#00D2B4" Stroke="#121212" StrokeThickness="2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Robust ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          Foreground="{TemplateBinding Foreground}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Padding="{TemplateBinding Padding}"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="false"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Margin="6,2,2,2" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                                <Path Name="Arrow" Grid.Column="1" Fill="#888888" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M0,0 L4,4 L8,0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#2A2A2A" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="#1A1A1A" BorderBrush="#444444" BorderThickness="1" CornerRadius="0,0,4,4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True" VerticalScrollBarVisibility="Auto">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="2">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#00D2B4"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#2A2A2A"/>
            <Setter Property="Margin" Value="0,15,0,15"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0">
            <TextBlock Text="oneDirectionCore" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,2"/>
            <TextBlock Text="Spatial Audio Radar Controller" Foreground="#888888" FontSize="12" Margin="0,0,0,5"/>
        </StackPanel>
        
        <Separator Grid.Row="1" Margin="0,5,0,20"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT COLUMN -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Hidden" Padding="0,0,15,0">
                <StackPanel>
                    <!-- Engine Section -->
                    <TextBlock Text="ENGINE" Style="{StaticResource Header}" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="BtnStart" Content="START ENGINE" Style="{StaticResource PrimaryButton}" Click="BtnStart_Click" Width="140"/>
                        <Button x:Name="BtnStop" Content="STOP" Click="BtnStop_Click" Width="80"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                        <Border x:Name="StatusIndicator" Width="12" Height="12" CornerRadius="6" Background="#555555" VerticalAlignment="Center" Margin="2,0,8,0"/>
                        <TextBlock x:Name="StatusLabel" Text="ENGINE STOPPED" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Separator Margin="0,25"/>

                    <!-- Audio Section -->
                    <TextBlock Text="AUDIO PIPELINE" Style="{StaticResource Header}" Margin="0,0,0,10"/>
                    
                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Input Channels" Margin="0,0,0,5"/>
                            <ComboBox x:Name="ComboChannels" Margin="0">
                                <ComboBoxItem IsSelected="True">Stereo (2.0)</ComboBoxItem>
                                <ComboBoxItem>Surround (5.1)</ComboBoxItem>
                                <ComboBoxItem>Surround (7.1)</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Detection Preset" Margin="0,0,0,5"/>
                            <ComboBox x:Name="ComboPreset" Margin="0">
                                <ComboBoxItem IsSelected="True">Standard / Raw Energy</ComboBoxItem>
                                <ComboBoxItem>PUBG Advanced</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Sensitivity" Grid.Column="0"/>
                                <TextBlock x:Name="LblSensitivity" Text="70" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderSensitivity" Minimum="0" Maximum="100" Value="70" ValueChanged="SliderSensitivity_ValueChanged"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Separation" Grid.Column="0"/>
                                <TextBlock x:Name="LblSeparation" Text="50" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderSeparation" Minimum="0" Maximum="100" Value="50" ValueChanged="SliderSeparation_ValueChanged"/>
                        </StackPanel>
                    </Grid>

                    <Separator Margin="0,20"/>

                    <!-- Hardware Section -->
                    <TextBlock Text="HARDWARE" Style="{StaticResource Header}" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Text="Serial COM Port" VerticalAlignment="Center"/>
                        <ComboBox x:Name="ComboPorts" Grid.Column="1" Width="140" Margin="0"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!-- RIGHT COLUMN -->
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Hidden" Padding="15,0,0,0">
                <StackPanel>
                    <!-- Display Section -->
                    <TextBlock Text="DISPLAY &amp; OVERLAY" Style="{StaticResource Header}" Margin="0,0,0,10"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <CheckBox x:Name="CheckFullscreen" Content="Full Screen Overlay" Margin="0,0,15,0"/>
                        <CheckBox x:Name="CheckTray" Content="Minimize to Tray" Margin="0,0,15,0"/>
                        <CheckBox x:Name="CheckStartup" Content="Launch on Startup"/>
                    </StackPanel>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Radar Position" Margin="0,0,0,5"/>
                            <ComboBox x:Name="ComboPosition" Margin="0">
                                <ComboBoxItem>Top-Left</ComboBoxItem>
                                <ComboBoxItem>Top-Center</ComboBoxItem>
                                <ComboBoxItem IsSelected="True">Top-Right</ComboBoxItem>
                                <ComboBoxItem>Bottom-Left</ComboBoxItem>
                                <ComboBoxItem>Bottom-Center</ComboBoxItem>
                                <ComboBoxItem>Bottom-Right</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                           <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                               <TextBlock Text="Radar HUD Size" Grid.Column="0"/>
                               <TextBlock x:Name="LblRadarSize" Text="250" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                           </Grid>
                           <Slider x:Name="SliderRadarSize" Minimum="100" Maximum="600" Value="250" ValueChanged="SliderRadarSize_ValueChanged" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Global Opacity" Grid.Column="0"/>
                                <TextBlock x:Name="LblGlobalOpacity" Text="100%" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderGlobalOpacity" Minimum="0" Maximum="100" Value="100" ValueChanged="SliderGlobalOpacity_ValueChanged"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Radar HUD Opacity" Grid.Column="0"/>
                                <TextBlock x:Name="LblRadarOpacity" Text="80%" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderRadarOpacity" Minimum="0" Maximum="100" Value="80" ValueChanged="SliderRadarOpacity_ValueChanged"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Dot Opacity" Grid.Column="0"/>
                                <TextBlock x:Name="LblDotOpacity" Text="100%" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderDotOpacity" Minimum="10" Maximum="100" Value="100" ValueChanged="SliderDotOpacity_ValueChanged"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Polling Rate" Grid.Column="0"/>
                                <TextBlock x:Name="LblPollRate" Text="144 Hz" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderPollRate" Minimum="30" Maximum="240" Value="144" ValueChanged="SliderPollRate_ValueChanged"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Zoom / Range" Grid.Column="0"/>
                                <TextBlock x:Name="LblRange" Text="100" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderRange" Minimum="1" Maximum="200" Value="100" ValueChanged="SliderRange_ValueChanged"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Max Entities" Grid.Column="0"/>
                                <TextBlock x:Name="LblMaxEntities" Text="6" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderMaxEntities" Minimum="1" Maximum="10" Value="6" ValueChanged="SliderMaxEntities_ValueChanged"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Smoothness" Grid.Column="0"/>
                                <TextBlock x:Name="LblSmoothness" Text="0.50" Grid.Column="1" Foreground="#00D2B4" FontWeight="Bold"/>
                            </Grid>
                            <Slider x:Name="SliderSmoothness" Minimum="0" Maximum="100" Value="50" ValueChanged="SliderSmoothness_ValueChanged"/>
                        </StackPanel>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Separator Grid.Row="3" Margin="0,15,0,15"/>
        <Grid Grid.Row="3" Margin="0,40,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Check for Updates" Click="BtnCheckUpdates_Click" Width="150" Height="34"/>
            </StackPanel>
            <Button Content="Exit Application" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#331111" Click="BtnExit_Click" Width="140" Height="34"/>
        </Grid>
    </Grid>
</Window>
